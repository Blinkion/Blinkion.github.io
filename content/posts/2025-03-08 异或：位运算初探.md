+++
date = '2025-03-08'
draft = false
title = '异或：位运算初探'
author = 'CSemy'
tags = ["算法","位运算","异或"]
+++

# 异或（Xor）

## 知识

+ 异或（Exclusive OR，简称 XOR）是一种**二元逻辑运算符**，数学符号为 $\bigoplus$，编程符号为 ^。其本质是**不进位二进制加法**，运算规则如下
$$
0 \bigoplus 0 = 0\\
1 \bigoplus 0 = 1\\
0 \bigoplus 1 = 1\\
1 \bigoplus 1 = 0\\
$$

## [例题](https://ac.nowcoder.com/acm/contest/25022/1002)

题意：某大学有 n 个职员，编号为 1 ~ N。他们之间有从属关系，也就是说他们的关系就像一棵以校长为根的树，父结点就是子结点的直接上司。现在有个周年庆宴会，宴会每邀请来一个职员都会增加一定的快乐指数 a_i，但是呢，如果某个职员的直接上司来参加舞会了，那么这个职员就无论如何也不肯来参加舞会了。所以，请你编程计算，邀请哪些职员可以使快乐指数最大，求最大的快乐指数。

+ 我们设二维dp数组代表以 i 为根的子树的所有最优解
+ 对于每个状态，都存在两种决策（为 0 代表 i 不参加舞会的情况，1 代表 i 参加舞会的情况）
+ 注意可以老板员工均不参加，所以老板和其员工的参与与否一共三种情况
+ 通过 DFS，在返回上一层时更新当前结点的最优解

```cpp
//AC code
#include <bits/stdc++.h>
using namespace std;
const int maxn = 6005;
vector<int> g[maxn];//链式前向星存图固然把空间利用到了极致，
//但在不卡stl时，使用vector+auto让代码更加简洁。
int dp[maxn][2], a[maxn], n;//dp数组第二维0表示不选取，1表示选取
void dfs(int u, int fa)
{
    dp[u][0] = 0;
    dp[u][1] = a[u];
    for (auto x : g[u])
    {
        if (x != fa)
        {
            dfs(x, u);
            dp[u][0] += max(dp[x][1],dp[x][0]);
            dp[u][1] += dp[x][0];
        }
    }
}
int main()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    for (int i = 1; i <= n; i++)
    {
        int x, y;
        if(x==0 and y == 0)
            break;
        cin >> x >> y;
        g[x].emplace_back(y);
        g[y].emplace_back(x);
    }
    dfs(1, -1);//初始入口，根节点无父亲，任意赋值
    cout << max(dp[1][0], dp[1][1]);
}

```

{{< code language="css" title="Really cool snippet" id="1" expand="Show" collapse="Hide" isCollapsed="true" >}}
pre {
  background: #1a1a1d;
  padding: 20px;
  border-radius: 8px;
  font-size: 1rem;
  overflow: auto;

  @media (--phone) {
    white-space: pre-wrap;
    word-wrap: break-word;
  }

  code {
    background: none !important;
    color: #ccc;
    padding: 0;
    font-size: inherit;
  }
}
{{< /code >}}